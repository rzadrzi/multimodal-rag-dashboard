services:
#  db:
#    image: postgres:latest
#    restart: always

  web:
    build: .
#      context: filemanager
#      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/app/